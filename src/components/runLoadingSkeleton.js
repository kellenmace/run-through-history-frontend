import styled from "styled-components"

const RunLoadingSkeleton = styled.div`
  height: 105.44px;
  width: 100%;
  background-color: var(--color-grey-8);
  border-radius: var(--border-radius);
  margin-bottom: 8px;
`

export default RunLoadingSkeleton
